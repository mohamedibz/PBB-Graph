{
  "name": "PBB-Graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ PBB-Graph",
    "create-local": "graph create --node http://localhost:8020/ PBB-Graph",
    "remove-local": "graph remove --node http://localhost:8020/ PBB-Graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 PBB-Graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.95.0",
    "@graphprotocol/graph-ts": "0.36.0",
    "PBB-Graph": "file:"
  }
}
